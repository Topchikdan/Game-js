export const button = document.querySelector(".clicker__button");
export const score = document.querySelector(".clicker__score");
export const progress = document.querySelector(".clicker__progress");
export const level = document.querySelector(".clicker__level-count");
export const bonusList = document.querySelector(".clicker__bonus-list");

// Глобальные настройки
export const clicker = {
    score: 0,
    progress: 0,
    level: 1,
    multiClick: 0,
}
